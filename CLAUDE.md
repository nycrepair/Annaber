# Project Instructions

## Pull Requests

After every set of changes (commit + push), always create a new pull request to `main` using the GitHub MCP tools. Do not wait to be asked. Do NOT merge the PR — leave it open for the user to merge themselves.
